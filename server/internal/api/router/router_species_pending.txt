=== AI 어종 식별 (P4-5) ===

router.go - Setup 함수 시그니처에 추가:
  speciesH *handler.SpeciesIdentifyHandler,

router.go - api 그룹에 추가 (공개 엔드포인트 - 선택적 인증):
  // ── AI 어종 식별 ────────────────────────────────────
  // 선택적 인증: 로그인 시 user_id 기록, 비로그인도 사용 가능
  api.POST("/species/identify", speciesH.Identify)

main.go - 초기화 추가:
  speciesIdentifySvc := service.NewSpeciesIdentifyService(db, cfg.AI.APIKey)
  speciesH := handler.NewSpeciesIdentifyHandler(speciesIdentifySvc)

main.go - router.Setup 호출에 speciesH 추가
